.footer
  .content
    アンケート作成にはログインする必要があります。
    - if user_signed_in?
      .content__logout
        = link_to destroy_user_session_path, method: :delete do
          logout
    - else
      .content__login
        = link_to new_user_session_path do
          login
