.footer
  .content
    アンケート作成をご希望の方はaaaaa.gmail.comへご連絡
    - if user_signed_in?
      .content__logout
        = link_to destroy_user_session_path, method: :delete do
          logout
    - else
      .content__login
        = link_to new_user_session_path do
          login
